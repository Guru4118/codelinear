/**
 * Framer Motion re-exports — import motion primitives from here for consistency.
 * Client components only; use in files marked with "use client".
 */
export {
  AnimatePresence,
  LazyMotion,
  domAnimation,
  domMax,
  m,
  motion,
  useReducedMotion,
} from "framer-motion";

export type {
  HTMLMotionProps,
  MotionProps,
  TargetAndTransition,
  Variants,
} from "framer-motion";
